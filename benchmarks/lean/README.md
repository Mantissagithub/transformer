# Lean Benchmark Reports

Reports generated by `python -m lean_sft.benchmark` land here.

Use Lean-verified pass rate as the real proof metric. Text-only generation metrics are reported separately when the Lean toolchain is unavailable.
